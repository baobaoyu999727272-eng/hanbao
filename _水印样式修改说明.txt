已知 VS Code apply_patch 工具无法识别一行内的 CSS 块，建议手动定位并替换。请将如下 CSS 粘贴到 <style> 内，替换原有 .watermark 样式：

.watermark {
  position: absolute;
  top: 18px;
  left: 22px;
  margin: 0;
  padding: 0;
  text-align: left;
  font-size: 1.25rem;
  color: #1a4a8a;
  opacity: 0.18;
  font-weight: 900;
  letter-spacing: 0.18em;
  user-select: none;
  pointer-events: none;
  z-index: 50;
}

并删除 <div class="apple-logo"></div> 这一行。
